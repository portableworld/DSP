<% form_for([@panel, @content]) do |f| %>
  <%= f.error_messages %>
<% if @panel.live_state == "Live" %>
  Should this
  <% if controller.action_name == "new" %>
  new
  <% else %>
  edited
  <% end %>
  content make its containing cell appear unread to everyone?<br />
<input id="unread_status" name="unread" type="radio" value="Yes" />Yes<br />
<input checked="checked" id="unread_status" name="unread" type="radio" value="No" />No<br />
<% end %>
  <p>
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </p>

  	<% if controller.action_name == "new" %>
    	<%= f.hidden_field :order, {:value => @content_count } %>
	<% else %>
		<%= f.hidden_field :order %>
	<% end %>


    <%= f.hidden_field :panel_id %>

  <p>
  	<% if controller.action_name == "new" %>
    	<%= f.submit 'Create' %>
	<% else %>
		<%= f.submit 'Update' %>
	<% end %>
  </p>
<% end %>